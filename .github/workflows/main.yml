name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: underarc-bot
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
